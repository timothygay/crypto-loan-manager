# Local-only backup of the whole pipeline — MUST NEVER be pushed to this PUBLIC repo.
# Contains Google Apps Script sources (settlement-alerts holds the Telegram bot
# token), the HANDOVER.md operational memory, and CSV/xlsx exports of live data.
_backup/

# Private working log — keep out of the PUBLIC repo
progress.md

# Secrets / env
.env
.env.*
*.local

# Vercel local link
.vercel/

# Deps / build
node_modules/

# OS junk
.DS_Store
.vercel

# Local dev/preview config
.claude/
